<%= simple_form_for([ attachment, comment],
                    data: {
                      insert_in_comments_text_target: "form",
                      action: "submit->insert-in-comments-text#send"
                    }) do |f| %>
  <%= f.input :content, as: :text %>
  <%= f.button :submit %>
<% end %>
